{
  "name": "@hot-updater/root",
  "private": true,
  "description": "React Native OTA solution for self-hosted",
  "keywords": [],
  "license": "MIT",
  "scripts": {
    "biome": "biome lint packages/** plugins/** --write --unsafe",
    "biome:format": "biome format packages/** plugins/** --write",
    "biome:check": "biome check packages/** plugins/**",
    "clear": "rimraf --glob **/dist .nx",
    "test": "vitest",
    "build": "nx run-many --target=build --projects=packages/*,plugins/*",
    "build:dev": "node scripts/build-dev.mjs",
    "fingerprint:create": "nx run-many --target='fingerprint:create' --projects=examples/*",
    "test:type": "nx run-many --target=test:type --projects=packages/*,plugins/*",
    "release:test": "nx release --dry-run",
    "release": "pnpm clear && pnpm build && nx release --skip-publish",
    "publish:all": "nx release publish",
    "publish:rc": "nx release publish --tag rc",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@changesets/cli": "^2.29.3",
    "@clack/prompts": "catalog:",
    "@cloudflare/vitest-pool-workers": "^0.8.70",
    "@nx/js": "^21.0.1",
    "@swc/helpers": "^0.5.17",
    "chokidar": "^4.0.3",
    "execa": "catalog:",
    "nx": "^21.0.1",
    "picocolors": "catalog:",
    "rimraf": "^5.0.7",
    "tsdown": "^0.15.9",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.18.1"
}
