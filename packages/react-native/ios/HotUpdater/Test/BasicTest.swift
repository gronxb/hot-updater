// Basic test file to ensure Swift package compiles correctly
// Full unit tests for iOS are run through React Native integration tests

import Foundation

// This file exists to ensure the Swift package builds successfully
// The actual unit tests are comprehensive but require React Native runtime
// which is not available in SPM context
