import type { HotUpdaterFeatureInvocation } from "@hot-updater/plugin-core";

type PublicFeature = Readonly<{
  summary(bundleId: string, context?: { readonly requestId: string }): string;
}>;

declare const publicFeature: PublicFeature;
declare const invocation: HotUpdaterFeatureInvocation<{
  readonly requestId: string;
}>;

publicFeature.summary("bundle", { requestId: "request" }, invocation);
