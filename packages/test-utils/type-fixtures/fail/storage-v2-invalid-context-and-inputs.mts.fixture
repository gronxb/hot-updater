import type {
  StorageOperationContext,
} from "@hot-updater/plugin-core/storage";

const wrongTarget: StorageOperationContext = {
  target: "browser",
  environment: {},
  bindings: {},
};
const missingEnvironment: StorageOperationContext = {
  target: "node",
  bindings: {},
};
const wrongEnvironment: StorageOperationContext = {
  target: "worker",
  environment: { TOKEN: 42 },
  bindings: {},
};
const missingBindings: StorageOperationContext = {
  target: "edge",
  environment: {},
};

void wrongTarget;
void missingEnvironment;
void wrongEnvironment;
void missingBindings;
