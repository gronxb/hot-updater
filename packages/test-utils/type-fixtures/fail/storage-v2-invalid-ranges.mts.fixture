import type {
  StorageByteRange,
  StorageContentRange,
} from "@hot-updater/plugin-core/storage";

const badStart: StorageByteRange = { start: "0" };
const badEnd: StorageByteRange = { start: 0, end: "1" };
const badRangeShape: StorageByteRange = { start: 0, length: 1 };
const badContentRange: StorageContentRange = {
  start: 0,
  end: 1,
  totalLength: "2",
};

void badStart;
void badEnd;
void badRangeShape;
void badContentRange;
