import type { HotUpdaterConsoleConfig } from "./hosted";

export const defineConfig = (
  config: HotUpdaterConsoleConfig,
): HotUpdaterConsoleConfig => config;

export type { HotUpdaterConsoleConfig };
