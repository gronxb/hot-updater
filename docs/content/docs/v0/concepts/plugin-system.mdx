---
title: "Plugin System"
description: "Hot Updater uses a flexible plugin system that lets you customize how bundles are built, stored, and managed to fit your infrastructure."
icon: Puzzle
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

## Build Plugin

Build plugins handle the bundling process when you run the `hot-updater deploy` command. They generate the JavaScript bundles for your updates.

### Supported Plugins

- **`@hot-updater/bare`**: Runs the project's configured bundler through the
  React Native CLI `bundle` command.
- **`@hot-updater/rock`**: Runs the project's configured bundler through the
  [Rock CLI](https://github.com/callstack/rock) `bundle` command.
- **`@hot-updater/expo`**: Executes the bundle command through Expo.


## Storage Plugin

Storage plugins upload your generated bundles during the `hot-updater deploy` command and store them in your chosen storage provider.

Storage plugins expose explicit environment profiles:

- `node`: deploy-time APIs for upload, delete, and local file downloads.
- `runtime`: update-check APIs for HTTP(S) download URLs and small metadata
  reads.

`hot-updater.config.ts` uses the `node` profile. `createHotUpdater` uses the
`runtime` profile. Most apps can use the official storage plugins directly;
custom plugin authors only need the profile details when implementing their own
storage provider.

### Supported Plugins

- **`@hot-updater/supabase`**: Stores bundles in Supabase with `supabaseStorage`.
- **`@hot-updater/cloudflare`**: Provides deploy-time `r2Storage` and Worker runtime storage for Cloudflare R2.
- **`@hot-updater/aws`**: Stores bundles in AWS S3 with `s3Storage`.
- **`@hot-updater/firebase`**: Stores update bundles in Firebase Storage with `firebaseStorage`.

## Database Plugin

Database plugins store bundle metadata that your app uses to check for updates. They run during the `hot-updater deploy` command.

### Supported Plugins

- **`@hot-updater/supabase`**: Stores metadata in Supabase with `supabaseDatabase`.
- **`@hot-updater/aws`**: Stores metadata in AWS with `s3Database`.
- **`@hot-updater/cloudflare`**: Stores metadata in Cloudflare D1 with `d1Database`.
- **`@hot-updater/firebase`**: Stores update metadata in Firebase Firestore with `firebaseDatabase`.
