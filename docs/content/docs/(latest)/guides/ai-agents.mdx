---
title: "Set up with an agent"
description: "Ask a coding agent to install v1, provision infrastructure, connect your app and verify an OTA update."
icon: Bot
---

## Complete setup with your agent

Use your coding agent to discover the app and existing infrastructure, complete
setup and report observable results. For terminal setup, use
[Set up manually](/docs/get-started/basic-usage). For a v0 app, follow
[Move from v0 to v1](/docs/guides/upgrade-to-v1) instead of a new-project setup.

## Attach the Skill

Install the [Hot Updater skill](https://github.com/hot-updater/skills/blob/main/skills/hot-updater/SKILL.md)
from the app's project directory:

```sh
npx skills add hot-updater/skills
```

The skill gives the agent the workflow. It does not install a newer CLI or
supply access to your provider account.

## Ask With `$hot-updater`

Ask for the complete setup and test:

```text
$hot-updater Set up this project with the published v1 rc packages and verify its first OTA update.
Inspect the app root, build system, installed packages and existing provider resources first.
Install hot-updater@rc, @hot-updater/react-native@rc and the required build/provider packages at @rc
before generating infrastructure. Record each resolved version; RC numbers can differ.
Use agent infra setup and its generated instructions to provision and verify the server.
Connect the app's base URL and client API key, including both native and OTA build environments.
Configure native bundle loading, compatibility and a dedicated test channel; preserve existing signing.
Build and launch a native Release app, publish a visible JavaScript and asset change to its exact scope,
and verify the changes after applying the OTA. Check the deployment ID, rollback and received Insights.
Report server verification, native launch, OTA application and remaining native release work separately.
```

The agent can leave the provider choice until after discovery. It reuses suitable
existing resources and creates missing projects, databases, buckets and services
when its tools and access permit it. If a choice remains unresolved, use
[Choose infrastructure](/docs/get-started/choose-provider).

The shared [installation contract](/docs/get-started/installation) includes the
CLI, SDK, selected build/provider packages and helper runtime prerequisites.
The agent checks the installed CLI's help before invoking commands.

## What the agent should complete

| Phase | Evidence to report |
| --- | --- |
| Inspect and install | App workspace, build system, provider and resolved package versions |
| Provision infrastructure | Actual resource identifiers, deployed base URL, server version and authenticated client request |
| Connect the app | Native loading, client key supplied to app builds, matching strategy/channel and signing key when enabled |
| Test OTA | Release app launch, visible JavaScript and asset changes after application, matching deployment ID and rollback result |
| Observe and hand off | Received Insights when enabled; remaining store/native release steps and any specific blockers |

[Connect your app](/docs/get-started/app-setup) is the canonical integration
procedure. [Test an OTA update](/docs/guides/simulator-test) defines the device
checks. A generated scaffold, a successful doctor check or an uploaded bundle
alone does not prove that a device applied an OTA update.

If the agent cannot build or interact with the native app, it should complete
available setup work and identify the exact remaining device check.

## Safety Rules

- Authenticate through the provider's login flow or private local/provider
  credential storage. Never paste tokens, passwords, private keys or credential
  JSON into the conversation. The agent verifies access without printing values.
- The app receives only its client API key. Provider, service-role, admin and
  signing credentials remain in the CLI/server or protected deployment job.
- The agent asks only for unresolved targets, missing login/access, billing
  activation or consequential choices it cannot infer. Existing authorization
  does not need to be repeated for every generated checklist step.
- For infrastructure, the agent generates files with `agent infra setup` or
  `agent infra upgrade`, then applies them through its available provider tools.
  [Agent Infrastructure](/docs/guides/agent-infrastructure) documents the output,
  versioned instructions and resume record.
- Before retrying a failed remote action, inspect actual remote state. Keep
  existing resource identities, secrets and migration history.
- Before a state-changing OTA operation, identify the public Bundle ID,
  platform, channel and affected compatibility/rollout scope. Verify the
  result afterward; do not silently broaden a failed deployment's scope.

## Upgrade existing infrastructure

For an existing v1 server, ask:

```text
$hot-updater Upgrade this project's existing server infrastructure using the published rc channel.
Read all applicable versioned upgrade instructions, preserve existing resources and customizations,
and verify the deployed endpoint and authenticated client access afterward.
```

The agent follows
[Upgrade an existing server](/docs/guides/agent-infrastructure#upgrade-an-existing-server).
For v0, use the [migration workflow](/docs/guides/upgrade-to-v1).

## Deploy and diagnose after setup

```text
$hot-updater Deploy the current iOS app version to the staging channel and verify the result.
$hot-updater List iOS Bundles on the production channel.
$hot-updater Inspect this Bundle ID and its affected scope, then disable it and verify rollback.
$hot-updater Run doctor for this project's configured server and fix local setup issues.
```

Use [Doctor](/docs/guides/doctor) for structured `--json` diagnostics. Its
`auto`, `command` and `blocked` labels describe repair prerequisites; the agent
can continue an authorized infrastructure repair when the necessary access is
available. Public Bundle IDs drive normal operations; Artifact IDs are for
advanced storage and native crash diagnostics.
