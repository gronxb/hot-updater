---
title: "Datadog"
description: "The `withDatadog()` plugin for hot-updater enables automatic sourcemap upload to Datadog during the update bundle build process."
icon: datadog
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

## Prerequisites

- Datadog Account: Sign up here if you don't have one.
- Auth Token: Generate a token from your Datadog dashboard. This must be defined as the environment variable `DATADOG_API_KEY`.
- Datadog site: specify the site for the API key with `DATADOG_SITE` (e.g., `DATADOG_SITE=datadoghq.eu`).
- Install the plugin:

```package-install
npm install @hot-updater/datadog-plugin @datadog/datadog-ci --save-dev
```

## Step 1: Wrap your build plugin

Use `withDatadog()` to wrap any compatible build plugin such as bare.

<Tabs items={['Bare', 'Expo', 'Rock']}>
  <Tab value="Bare">

```ts title="hot-updater.config.ts"
import { bare } from "@hot-updater/bare";
import { withDatadog } from "@hot-updater/datadog-plugin"; // [!code ++]
import { defineConfig } from "hot-updater";
import { config } from "dotenv";

config({ path: ".env.hotupdater" });

export default defineConfig({
  build: withDatadog( // [!code ++]
    bare({
      enableHermes: false, // or true, depending if you want to use it
      // Required for sourcemap upload
      sourcemap: true, // [!code ++]
    }),
    { // [!code ++]
      // Sort sourcemaps for the same version by this build number.
      buildNumber: "build number", // [!code ++]
      // Match the release version used by the app's logs.
      releaseVersion: "release version", // [!code ++]
      // Datadog service associated with these sourcemaps.
      service: "service", // [!code ++]
    }, // [!code ++]
  ), // [!code ++]
  // .. your other config
});
```
</Tab>
<Tab value="Expo">

```ts title="hot-updater.config.ts"
import { withDatadog } from "@hot-updater/datadog-plugin"; // [!code ++]
import { defineConfig } from "hot-updater";
import { expo } from "@hot-updater/expo";

export default defineConfig({
  build: withDatadog( // [!code ++]
    expo({
      // Required for sourcemap upload
      sourcemap: true, // [!code ++]
    }),
    { // [!code ++]
      // Sort sourcemaps for the same version by this build number.
      buildNumber: "build number", // [!code ++]
      // Match the release version used by the app's logs.
      releaseVersion: "release version", // [!code ++]
      // Datadog service associated with these sourcemaps.
      service: "service", // [!code ++]
    }, // [!code ++]
  ), // [!code ++]
  // .. your other config
});
```
</Tab>

<Tab value="Rock">

```ts title="hot-updater.config.ts"
import { withDatadog } from "@hot-updater/datadog-plugin"; // [!code ++]
import { defineConfig } from "hot-updater";
import { rock } from "@hot-updater/rock";

export default defineConfig({
  build: withDatadog( // [!code ++]
    rock({
      // Required for sourcemap upload
      sourcemap: true, // [!code ++]
    }),
    { // [!code ++]
      // Sort sourcemaps for the same version by this build number.
      buildNumber: "build number", // [!code ++]
      // Match the release version used by the app's logs.
      releaseVersion: "release version", // [!code ++]
      // Datadog service associated with these sourcemaps.
      service: "service", // [!code ++]
    }, // [!code ++]
  ), // [!code ++]
  // .. your other config
});
```

</Tab>

</Tabs>

<Callout title="Automatic Upload">
`withDatadog()` uploads all generated sourcemaps to Datadog during `hot-updater deploy`.
</Callout>

## Step 2: Deploy

Deploy the update to build the bundle and upload its sourcemaps to Datadog.

```package-install
npx hot-updater deploy -i
```

## Verify symbolication

Install a release build, deploy a small JavaScript change with sourcemaps
enabled, and trigger a controlled test error from the updated code. In your
monitoring dashboard, confirm the stack points to the original source file
and line in that update. A successful upload alone does not prove the app and
uploaded map use the same identity.

Match the runtime SDK's service, release version and build number to the values
passed to `withDatadog`. Configure that SDK through your existing Datadog app
integration before performing the test.
