---
title: "Supabase Database"
description: Store your Hot Updater bundle metadata in Supabase PostgreSQL database.
icon: supabase
---

## Installation

```package-install
npm install @hot-updater/supabase@rc --save-dev
```

## Setup

Set up the backend with an [AI agent or the interactive
CLI](/docs/managed/supabase#step-2-configure-supabase), then use the
configuration below.

The settings below connect to an initialized database; they do not create
its tables or RPC functions. Complete provider setup first, or apply the
SQL migrations from the installed `@hot-updater/supabase` package's
`supabase/migrations` directory to the selected project before using the plugin.

## Configuration

```typescript
interface SupabaseDatabaseConfig {
  supabaseUrl: string; // Your Supabase project URL
  supabaseServiceRoleKey: string; // Your Supabase service role or secret key
}
```

## Usage

```typescript
import { supabaseDatabase } from "@hot-updater/supabase";
import { defineConfig } from "hot-updater";

export default defineConfig({
  database: supabaseDatabase({
    supabaseUrl: process.env.SUPABASE_URL,
    supabaseServiceRoleKey: process.env.SUPABASE_SERVICE_ROLE_KEY,
  }),
  // ... other config
});
```

## Edge Runtime Usage

Use the `/edge` subpath when composing `createHotUpdater` inside a Supabase
Edge Function. The provider keeps the same name while the subpath makes the
runtime boundary explicit:

```typescript
import { createHotUpdater } from "@hot-updater/server";
import { supabaseDatabase } from "@hot-updater/supabase/edge";

export const hotUpdater = createHotUpdater({
  database: supabaseDatabase({
    supabaseUrl: Deno.env.get("SUPABASE_URL")!,
    supabaseServiceRoleKey: Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,
  }),
  clientAccess: { type: "api-key" },
  // ... other options
});
```

## Complete Example

Combined storage and database:

```typescript
import { supabaseStorage, supabaseDatabase } from "@hot-updater/supabase";
import { defineConfig } from "hot-updater";

export default defineConfig({
  storage: supabaseStorage({
    supabaseUrl: process.env.SUPABASE_URL,
    supabaseServiceRoleKey: process.env.SUPABASE_SERVICE_ROLE_KEY,
    bucketName: "hot-updater-bundles",
  }),
  database: supabaseDatabase({
    supabaseUrl: process.env.SUPABASE_URL,
    supabaseServiceRoleKey: process.env.SUPABASE_SERVICE_ROLE_KEY,
  }),
  // ... other config
});
```
