---
title: "AWS DynamoDB + S3 + Lambda@Edge"
description: "Set up DynamoDB metadata, S3 bundle storage, CloudFront, and Lambda@Edge for Hot Updater."
icon: aws
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { Accordion, Accordions } from "fumadocs-ui/components/accordion";

## Overview

Use this preset to provision an OTA server on AWS. For a new app,
start with [Choose infrastructure](/docs/get-started/choose-provider). Provision
the provider resources here, then connect your app with the verified URL and key.

## Prerequisites

You need the following resources and access. An AI agent can prepare missing
resources through its available provider tools.

- **Node.js**: The CLI requires 20.19+. Agent setup uses a client-key helper that
  requires Node 22.18+ or Node 24+; prepare it before provisioning resources.
- **AWS Account**: Sign up at [AWS](https://aws.amazon.com) if you don't have one.
- **AWS CLI (terminal setup)**: Install the AWS CLI and sign in or configure a profile.

## Required AWS permissions

Hot Updater requires specific AWS IAM permissions for setup and ongoing usage:

### 1. Initialization (one-time setup)

Used for `hot-updater init`:

- `AmazonS3FullAccess`: Create and read S3 buckets.
- `AmazonDynamoDBFullAccess_v2`: Create and configure the metadata table.
- `AWSLambda_FullAccess`: Create and update Lambda functions.
- `CloudFrontFullAccess`: Manage CloudFront distributions.
- `IAMFullAccess`: Create IAM roles for Lambda@Edge.
- `AmazonSSMFullAccess`: Access SSM Parameters used for CloudFront key pairs.

### 2. Ongoing usage

Used for `hot-updater deploy` and `hot-updater console`:

- `AmazonS3FullAccess`: Manage bundle artifacts in the S3 bucket.
- DynamoDB table access for metadata queries and writes.
- `cloudfront:CreateInvalidation`: Invalidate CloudFront cache after deployments.

For ongoing usage, create a separate access token with limited permissions. Here's a minimal policy example for CloudFront invalidation:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["cloudfront:CreateInvalidation"],
      "Resource": "arn:aws:cloudfront::<ACCOUNT_ID>:distribution/<DISTRIBUTION_ID>"
    }
  ]
}
```

If this permission is missing, you may encounter the following error:

```
AccessDenied: User: arn:aws:sts::***:assumed-role/***/*** is not authorized to perform: cloudfront:CreateInvalidation on resource: arn:aws:cloudfront::***:distribution/*** because no identity-based policy allows the cloudfront:CreateInvalidation action
```

## Step 1: Install required packages

Complete [Install packages](/docs/get-started/installation) in the React
Native project before running init or an agent scaffold. Install the CLI, React
Native SDK, and the build plugin that matches your app, then install this provider:

```package-install
npm install @hot-updater/aws --save-dev
```

An agent can perform these steps after inspecting the app's package manager
and build configuration.

## Step 2: Configure AWS

Choose how to set up AWS:

<Tabs items={["AI agent", "Terminal"]}>
  <Tab value="AI agent">

Install the [Hot Updater skill](/docs/guides/ai-agents), then ask your coding agent:

```text
$hot-updater Set up infrastructure on AWS for this project.
```

The agent inspects your app, discovers existing configuration and resources,
and asks only for missing choices or access. It uses `hot-updater agent infra setup`
to generate the deployment templates, then applies and verifies them with available
provider tools. If authentication is needed, sign in through the provider or save
credentials in a local ignored file; do not paste secrets into the conversation.
See [Agent Infrastructure](/docs/guides/agent-infrastructure) for setup and resume.

  </Tab>
  <Tab value="Terminal">

Run the interactive initializer from your app directory:

```package-install
npx hot-updater init
```

<Accordions type="single">
  <Accordion title="Interactive Setup Steps">

1. Choose a build plugin for your project, such as Bare for React Native CLI projects.
2. Select AWS + Lambda@Edge as the provider for handling updates.
3. Select current AWS CLI/default session, a shared AWS profile, account access keys, or an AWS SSO profile
4. Provide a profile name for shared-profile or SSO mode, or credentials for account/access-key mode
5. Choose an AWS region for S3 and DynamoDB
6. Reuse an existing S3 bucket or create a new one
7. Enter a DynamoDB table name; init creates it or validates its schema
8. Create the first client API key or reuse the saved key
9. Create or reuse a resource-scoped IAM role for this Lambda installation
10. Deploy the Lambda@Edge function to handle updates
11. Create a new CloudFront distribution by default, or update the exact distribution saved by a previous init

  </Accordion>
</Accordions>

<video src={"/docs/get-started/init-aws.mov"} controls autoPlay loop muted />

  </Tab>
</Tabs>

After setup, review the local configuration. Interactive init writes the settings
below to `.env.hotupdater`; an agent merges the needed settings into your project.
Resource selections and the authentication mode are persisted for deterministic
reruns. AWS credentials are included only for account/access-key mode and only
when you consent to storing them.

```
HOT_UPDATER_DYNAMODB_TABLE_NAME=hot-updater-v1
HOT_UPDATER_AWS_AUTH_MODE=local-session
HOT_UPDATER_AWS_LAMBDA_NAME=hot-updater-v1-edge
HOT_UPDATER_S3_BUCKET_NAME=your-s3-bucket-name
HOT_UPDATER_S3_REGION=your-region
HOT_UPDATER_CLOUDFRONT_DISTRIBUTION_ID=your-distribution-id
HOT_UPDATER_API_KEY=your-api-key
```

Managed init reuses `HOT_UPDATER_API_KEY` when supplied or creates an API key,
registers it in DynamoDB, and persists the plaintext value locally. Send this
API key in the app's `x-api-key` request header. It does not authorize
management or Insights-query routes. Do not run `hot-updater api-key create`
for a managed installation; that command bootstraps direct self-hosted
`createHotUpdater` configurations.

If you choose a shared AWS profile or SSO, the generated env file also includes:

```bash
HOT_UPDATER_AWS_PROFILE=your-profile-name
```

If you choose the current AWS CLI/default session mode, no extra credential env
variables are written. The generated config uses the AWS SDK default provider
chain directly.

If you choose account/access-key mode and consent to persistence, the generated
env file also includes:

```bash
# The generated key may be over-privileged or temporary depending on how you authenticated during init.
HOT_UPDATER_S3_ACCESS_KEY_ID=your-access-key-id
HOT_UPDATER_S3_SECRET_ACCESS_KEY=your-secret-access-key
```

<Callout type="warn">

If you're **not** using the `react-native-dotenv` solution, provider credentials from `.env.hotupdater` will not be included in your app bundle. The API key is the exception: add it explicitly to the app as shown below.

See [Security](/docs/policy/security#plugin-token-protection).

</Callout>

## Optional: Bundle signing

Managed AWS init does not create or enable a signing key. The
[built-in local config](/docs/guides/bundle-signing#local-private-key) works
without a signing plugin. For non-exportable key custody, use the
[AWS KMS signer](/docs/guides/bundle-signing#hardened-aws-kms) from
`hot-updater/signing`; signing is independent of storage and database choice.
Embed the chosen public key in a native release before deploying signed
bundles. Signing credentials belong in the deploy environment, never in the
React Native app.

## Step 3: Add HotUpdater to your project

After provisioning, record these outputs for [Set up your app](/docs/get-started/app-setup):

| Output | Value |
| --- | --- |
| Client base URL | `https://<your-cloudfront-distribution-url>` |
| Client request header | `x-api-key` |
| Client API key | The saved `HOT_UPDATER_API_KEY` in your ignored `.env.hotupdater` |
| Deploy configuration | `hot-updater.config.ts` and the selected provider resources |

Use the actual URL returned by deployment. Configure the SDK and native bundle
loader through [Set up your app](/docs/get-started/app-setup), using the selected
build plugin and the same update strategy as `hot-updater.config.ts`. Only the
client API key belongs in the mobile app's build-time configuration; provider
credentials stay in the deploy environment.

<Callout type="info">
  The managed AWS runtime exposes `/version` publicly. OTA update checks and
  Insights ingestion require `x-api-key: $HOT_UPDATER_API_KEY`, using a key
  registered or created during init. Internally, Insights is always available
  and `clientAccess` uses the `api-key` policy; only `handlers.client` is
  mounted. API keys do not authorize Insights query routes, and bundle
  management endpoints are not mounted on the public function URL.
</Callout>

## Cost and capacity

Managed AWS init uses DynamoDB on-demand capacity, so an idle installation has
no provisioned read/write capacity charge. Release-catalog requests use a
dedicated CloudFront behavior whose cache key contains the canonical path,
`x-api-key`, and supported content encoding. It excludes SDK version,
Authorization, cookies, query strings, and installation state. A cold origin
fill performs one exact DynamoDB catalog read; a warm CloudFront hit skips both
Lambda@Edge and DynamoDB. Each authenticated Insights event invokes
Lambda@Edge and writes the immutable event plus the installation's latest-report
pointer in one DynamoDB transaction. Server-side Insights queries read cursor
pages or canonical latest-report rows; they do not materialize the raw event
log.
Review the [DynamoDB pricing model](https://aws.amazon.com/dynamodb/pricing/) and
[Lambda@Edge pricing](https://aws.amazon.com/lambda/pricing/).

Each API key authorizes OTA selectors and event ingestion, but not
Insights query or management routes. Managed tables cap on-demand throughput
at 4,000 reads and 100 writes per second for both the table and update index.
The cap is a cost guardrail and can throttle bursts; protect production
distributions with AWS WAF rate-based rules and configure AWS Budgets for a
monthly spending threshold. The AWS adapter does not delete Insights data
automatically. Event history is keyset-paginated. Reporting-installation counts
read all canonical installation rows, including inactive rows; bundle-outcome
counts consume the matching indexed range.

Bundle metadata has no fixed bundle, patch, or patch-relationship count limit.
Each serialized metadata item is limited to 8 KiB. The database maintains counters
and relation counts in the same transactions as metadata writes. Cursor pages
and exact/single-owner hydration use targeted key/index reads; owner-index keys
are hydrated from strongly consistent base-table rows and unprocessed batch keys
use bounded exponential backoff. Unfiltered totals use the transactionally
maintained metadata counter. Broad filtered scans paginate through all matching
metadata and may be throttled by the managed table's throughput cost guardrail.
DynamoDB's 100-action transaction service limit applies to the complete
`commit({ changes, expectations })` envelope, including cascade deletions.

New tables enable deletion protection. Init validates the billing mode,
throughput caps, string key types, and index layout before reusing an existing
table, then enables point-in-time recovery on both new and compatible reused
tables. Point-in-time recovery is a billed DynamoDB feature. Init does not
change deletion protection on an operator-owned reused table.

New distributions use `PriceClass_All` to preserve worldwide OTA latency. The
Release-catalog behavior honors the server's five-second shared TTL. Catalog
authentication failures, missing rows, and server errors are private and are
not cached. Cache identity uses only the API key and content encoding.

If regional reach matters less than transfer price, change the distribution price
class in AWS after init; later init runs preserve that setting. Init invalidates
only `/version`, `/events`, `/release-catalogs/*`, and `/artifacts/*`, leaving
immutable bundle caches warm.
CloudFront provides the first 1,000 invalidation paths per account each month
at no extra charge; see [invalidation pricing](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PayingForInvalidation.html).

Rerunning init preserves unrelated CloudFront origins/cache behaviors and S3
bucket-policy statements. Lambda roles and signing keys are isolated by the
Lambda name and grant access only to the selected bucket, SSM parameter,
DynamoDB table, and update index. Init provisions these resources through AWS
APIs rather than a CloudFormation stack, so deletion and rollback remain
operator-managed.

Init checks the selected distribution's `/version` response before writing env
files or changing AWS resources. Incompatible infrastructure is rejected before
any mutation.

Without `basePath`, a storage plugin treats the whole bucket as owned by its
configured database. Do not run `hot-updater storage prune --yes` while the
bucket is also used by another active Hot Updater database. Give each active
database a distinct `basePath` before using prune.

## Generated configuration and operations

Init generates `hot-updater.config.ts` with your AWS configuration.

If you choose **current AWS CLI/default session**, the generated config uses the
AWS SDK default provider chain instead of embedding access keys:

```ts title="hot-updater.config.ts"
import { bare } from "@hot-updater/bare";
import { fromNodeProviderChain } from "@aws-sdk/credential-providers";
import { dynamoDB, s3Storage } from "@hot-updater/aws";
import { defineConfig } from "hot-updater";
import { config } from "dotenv";

config({ path: ".env.hotupdater" });

const awsOptions = {
  region: process.env.HOT_UPDATER_S3_REGION!,
  credentials: fromNodeProviderChain(),
};

const storageOptions = {
  ...awsOptions,
  bucketName: process.env.HOT_UPDATER_S3_BUCKET_NAME!,
};

export default defineConfig({
  build: bare({ enableHermes: true }),
  updateStrategy: "appVersion",
  storage: s3Storage(storageOptions),
  database: dynamoDB({
    ...awsOptions,
    tableName: process.env.HOT_UPDATER_DYNAMODB_TABLE_NAME!,
    // prettier-ignore
    cloudfrontDistributionId: process.env.HOT_UPDATER_CLOUDFRONT_DISTRIBUTION_ID!,
  }),
});
```

If you choose **shared AWS profile**, `fromNodeProviderChain()` is replaced with
`fromIni({ profile: process.env.HOT_UPDATER_AWS_PROFILE! })`. SSO uses
`fromSSO({ profile: process.env.HOT_UPDATER_AWS_PROFILE! })`; only
account/access-key mode emits a literal access-key credential block.

### Replace account access keys (optional)

By this point, `.env.hotupdater` already contains the settings selected during
`hot-updater init`.

This step applies only if you selected **account/access-key mode** during
`init`. Current AWS CLI/default session, shared-profile, and SSO modes use their
generated credential providers and do not read these two variables.

The account-mode credentials entered during infrastructure setup may have
broader permissions than ongoing deployment requires.

Replace those credentials with a permanent least-privilege identity that can
write bundle artifacts to S3, read and write the selected DynamoDB table, and
invalidate the CloudFront distribution.

```bash title=".env.hotupdater"
HOT_UPDATER_S3_ACCESS_KEY_ID=your-access-key-id
HOT_UPDATER_S3_SECRET_ACCESS_KEY=your-secret-access-key
```

## Step 4: If you use Expo, add the config plugin

Follow the Expo path in [Set up your app](/docs/get-started/app-setup). It applies
the config plugin and generates the native integration. For DOM components,
continue with [Expo DOM components](/docs/build-plugins/expo#expo-dom-components-use-dom).

## Step 5: Add native code

For Bare or Rock, follow the Android and iOS instructions in
[Set up your app](/docs/get-started/app-setup). Keep the integration appropriate
to your React Native version and rebuild the native app after changing it.

## Upgrade infrastructure

With the [Hot Updater skill](/docs/guides/ai-agents) attached, ask your agent:

```text
$hot-updater Upgrade this project's existing AWS server infrastructure.
```

The agent discovers the existing deployment, uses `hot-updater agent infra upgrade`,
and reads the applicable version files before changing resources. It applies pending
changes and verifies the server with doctor. See
[Upgrade an existing server](/docs/guides/agent-infrastructure#upgrade-an-existing-server)
for the workflow or [Doctor](/docs/guides/doctor#update-outdated-infrastructure)
for terminal instructions.

For server templates to apply with your own deployment tooling, use
`hot-updater infra scaffold --provider aws`. See
[Infrastructure Templates](/docs/guides/infrastructure-templates).

## Verifying the setup

1. Confirm the selected AWS resources and the deployed client base URL.
2. From the React Native project, check the server and native integration:

   ```bash
   npx hot-updater doctor --server-base-url <client-base-url> --json
   ```

   Doctor checks the public `/version` endpoint. It does not prove an
   authenticated update download or application.
3. Complete [Test an OTA update](/docs/get-started/test-ota) on a release build.
   Verify that the app downloads and runs the changed code, then survives a restart.

An agent should report the selected resources, verified URL, configuration file
paths and verification result. Report a missing access, billing or native-build
prerequisite explicitly instead of marking an untested installation complete.

## Host the Console

Host the console on **Vercel, Netlify, or Docker/Node** with AWS SDK credentials
for the existing DynamoDB and S3 resources. ECS/Fargate is another option when
you want an AWS task role. See the
[console hosting matrix](/docs/guides/console-deployment#choose-a-host).
