{
  "name": "unit-native",
  "version": "0.21.0",
  "private": true,
  "description": "Native unit tests for @hot-updater/react-native",
  "scripts": {
    "test:ios": "cd ios && mise exec -- tuist install && mise exec -- tuist test",
    "test:android": "cd android && ./gradlew test",
    "test": "pnpm test:ios && pnpm test:android"
  },
  "engines": {
    "node": ">=18"
  }
}
