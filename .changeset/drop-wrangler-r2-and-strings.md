---
"@hot-updater/cloudflare": minor
"@hot-updater/plugin-core": minor
"@hot-updater/cli-tools": patch
"@hot-updater/react-native": minor
"hot-updater": minor
---

Require R2 S3 credentials, drop Wrangler `r2Storage` and Android `stringResourcePaths`. Doctor only targets infrastructure generation 1.0.0. Channel, fingerprint, and signing keys live in AndroidManifest.xml.
