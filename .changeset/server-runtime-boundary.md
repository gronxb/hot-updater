---
"@hot-updater/server": minor
"hot-updater": minor
---

Make the `@hot-updater/server` root export runtime-safe and move database generation, migration, and bundle diff APIs to `@hot-updater/server/node`.
