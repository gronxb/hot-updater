---
"@hot-updater/lynx": minor
---

Add a framework-independent Lynx build adapter with app-owned compilation or
prebuilt output, explicit entry and native compatibility metadata, and opaque
file preservation. Add pinned ReactLynx, VueLynx and OctaneLynx example fixtures
and native integration probes. Runtime installation and recovery remain under
implementation; this changeset does not represent completed OTA acceptance.
