---
"@hot-updater/core": patch
"@hot-updater/server": patch
"hot-updater": patch
---

Fix standalone database generation for provider SQL output and generated schema regeneration, and centralize the generated DB schema artifact contract.
