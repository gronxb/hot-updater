---
"@hot-updater/standalone": patch
---

Remove Standalone CRUD query emulation and translate domain reads directly to HTTP while retaining finite-ID reads, exact windows, reverse patch lookup and custom routes.
