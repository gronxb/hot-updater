---
"hot-updater": patch
---

Use the console update ID in deploy, promote, and release output. Open the committed update directly after interactive deployment, remove file and catalog identifiers from normal summaries, and label explicit artifact operations with File ID. Existing command flags and raw JSON remain unchanged.
