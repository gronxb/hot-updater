---
"@hot-updater/plugin-core": patch
---

Validate report section page requests and bind their cursors to the immutable
publication, section, metric and exact bundle filter. Preserve opaque identifiers
and integer bookmark precision while allowing the page size to change.
