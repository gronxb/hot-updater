appId: ${APP_ID}
---
- scrollUntilVisible:
    element:
      id: runtime-bundle-id
    direction: UP
    timeout: 10000
- runFlow:
    file: assert-text-by-id.yaml
    env:
      APP_ID: ${APP_ID}
      ASSERTION_LABEL: Crashed bundle summary matches expected value
      EXPECTED_TEXT: ${EXPECTED_CRASHED_BUNDLE_RESULT}
      TARGET_ID: launch-crashed-bundle-result
