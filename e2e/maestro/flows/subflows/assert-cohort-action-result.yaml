appId: ${APP_ID}
---
- runFlow:
    file: navigate-action-results.yaml
- retry:
    maxRetries: 5
    commands:
      - assertVisible:
          id: cohort-action-result
      - copyTextFrom:
          id: cohort-action-result
      - assertTrue:
          condition: ${maestro.copiedText.startsWith(EXPECTED_COHORT_ACTION_RESULT)}
          label: Cohort action result matches expected value
