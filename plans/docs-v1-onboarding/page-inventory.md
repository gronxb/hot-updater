# Complete v1 page inventory

Baseline: `next` at `ac67b805583b423d556ea0d62650b4cd2aa5006b`. All **86/86** existing latest pages were read and assessed individually. Paths are relative to `docs/content/docs/(latest)/`. The first OTA guide moves to `get-started/test-ota.mdx`; other baseline page URLs remain. Pre-release URL compatibility does not constrain the structure. The final column records this PR's accepted disposition, including deliberately retained or deferred content; the assessment column is the original audit evidence. See [Decisions](decisions.md) for objections and rejected alternatives.

## Baseline pages

| Page | Purpose / persona | Audit assessment | Accepted disposition in this PR |
| --- | --- | --- | --- |
 | `build-plugins/bare.mdx` | Configure RN CLI bundling; app maintainer | 46 focused lines and accurate options; minification wording at 45 ambiguous (“handled by Hermes (disabled when Hermes is enabled)”). | Keep focused build reference; use untagged stable install commands, clarify Hermes/Metro minification. |
 | `build-plugins/expo.mdx` | Configure Expo build + native config + DOM; Expo maintainer | 206 lines; basic build options are 12–36, DOM patch occupies 69–198 and is only for SDK <55. | Keep focused build reference and anchored DOM recipe; use untagged stable install commands. Additional DOM-page split deferred. |
 | `build-plugins/rock.mdx` | Configure Rock build; Rock maintainer | 44 focused lines; distinct hermes option and defaults match `plugins/rock/src/index.ts:75+`. | Keep focused build reference; use untagged stable install commands. Do not merge with Bare. |
 | `concepts/automatic-rollback.mdx` | Understand/test native startup recovery | Core mechanism and exact Artifact ID blocking distinguish it from operational disable (111–128). Strong keeper; API samples repeat reference but lifecycle explanation useful. | Keep native recovery explanation and URL; move navigation to Operate, distinct from server policy rollback. |
 | `concepts/how-it-works.mdx` | Evaluate / understand Bundle identity, infrastructure and rollback | Strong concise v1 model (9–16, 39–62); keep Bundle vs Artifact distinction. Infrastructure link points to fragile old setup fragment (30). | Keep under Concepts; correct server-catalog/client-selection ownership and link startup/active-state verification. |
 | `concepts/plugin-system.mdx` | Choose/extend build, storage, database, signing plugins | Package decomposition is reference context, not first-run dependency. Signing protocol detail (23–46) dominates supported build overview. | Keep under Concepts; describe database deployment/runtime ownership and the Integration plugin concern; deeper signing protocol trimming deferred. |
 | `custom/api-key-authentication.mdx` | Bootstrap/rotate client keys; managed and custom operators | Distinguishes all key boundaries and managed bootstrap correctly; line 57 universally recommends `db migrate` although Drizzle/Prisma reject it. | Keep bootstrap/rotation task under Security; replace universal db migrate with adapter-specific schema preparation. |
 | `custom/cli-configuration.mdx` | Connect RN deploy tooling to existing server; operator | Configuration at 23–55 is task-specific. Options, secure/alternate authentication, custom routes and endpoint table at 70–178 duplicate `database-plugins/standalone`. | Keep one complete server-to-CLI connection task; merge options/HTTP-reference ownership into existing standalone page via links. |
 | `custom/database/drizzle.mdx` | Integrate Drizzle schema/adapter; existing Drizzle owner | Schema generation/Drizzle Kit flow is unique (64–100), while CLI configuration (102–145) and client endpoint table (146+) repeat unrelated layers. | Keep adapter-specific schema/runtime recipe; merge repeated CLI config into the canonical connection task via direct links; use untagged stable install commands. |
 | `custom/database/kysely.mdx` | Integrate Kysely and migrations; existing Kysely owner | Generate/migrate distinction (62–92) is essential; CLI configuration from 94 and endpoints from 138 duplicate shared docs. | Keep adapter-specific schema/runtime recipe; merge repeated CLI config into the canonical connection task via direct links; use untagged stable install commands. |
 | `custom/database/mongodb.mdx` | Integrate MongoDB with atomic writes; MongoDB owner | Replica set/sharded cluster requirement and `transactions: true` at 60 are consequential and must be found before install; rest repeats CLI/routes from 77. | Keep adapter-specific schema/runtime recipe; merge repeated CLI config into the canonical connection task via direct links; use untagged stable install commands. |
 | `custom/database/prisma.mdx` | Integrate Prisma/generated schema; existing Prisma owner | Prisma 6.19.3 initial-client bootstrap and query semantics are unique; CLI block from 103/endpoints from 147 repeat all other adapters. | Keep adapter-specific schema/runtime recipe; merge repeated CLI config into the canonical connection task via direct links; use untagged stable install commands. |
 | `custom/database/sql-export.mdx` | Export SQL without config; DBA/infra agent | 76 focused lines; dialect and overwrite semantics differ from ORM generation and solve a separate job. | Keep focused reference and existing URL; reachable through task-oriented navigation. |
 | `custom/frameworks/elysia.mdx` | Mount scoped admin sub-app; Elysia owner | Bearer plugin and scoped hook differ from Hono/Express; optional Insights table at 100 is stale. | Keep framework-specific authentication and mounting; replace optional-Insights wording and duplicate mobile snippets with canonical app/CLI links. |
 | `custom/frameworks/express.mdx` | Mount Node adapter and protect handlers; Express owner | `toNodeHandler`, JSON body parser and mount-local auth are unique; optional Insights table at 111 is stale. | Keep framework-specific authentication and mounting; replace optional-Insights wording and duplicate mobile snippets with canonical app/CLI links. |
 | `custom/frameworks/hono.mdx` | Mount/authenticate handlers; Hono owner | Auth ordering is framework-specific and useful; route table at 101 calls Insights optional; RN `wrap` excerpt at 120 is unrelated duplicate and incomplete JSX. | Keep framework-specific authentication and mounting; replace optional-Insights wording and duplicate mobile snippets with canonical app/CLI links. |
 | `custom/hosting/docker.mdx` | Build/run custom server container; server deployer | 352 lines reimplement DB/server/CLI, but CJS output layout, `.dockerignore`, native driver/package roles are host-specific; `compose up` precedes schema setup at 290–300. | Keep runnable Docker deployment; consolidate CLI continuation, order schema before application startup, require HTTPS ingress for release app. |
 | `custom/hosting/vercel.mdx` | Deploy custom server on Vercel/Postgres; server deployer | Correct transaction-capable `pg` driver and env note; lines 188+ repeat RN CLI block; `.env.local` explicitly not loaded by hot-updater. | Keep Vercel deployment; consolidate CLI/app continuation and explicitly install the CLI for schema tooling. |
 | `custom/overview.mdx` | Choose self-hosting and compose existing stack; backend owner | Starts with two package-install blocks, then repeats CLI configuration at 33–73, detailed URI routing, handler policy and four ORM/three framework choices. | Rewrite as custom-server decision/architecture/schema map; remove repeated full app CLI tutorial and route to canonical tasks. |
 | `custom/quick-start.mdx` | One runnable self-hosted vertical slice; first-time backend owner/agent | Hono + SQLite + R2 setup has schema/key/bootstrap, but title omits chosen stack; next steps at 284 send back to already-completed CLI config; no first OTA success evidence. | Keep runnable Hono/SQLite/R2 server and CLI slice; merge native app instructions into app-setup. Require reachable HTTPS before release OTA test. |
 | `database-plugins/aws.mdx` | Configure DynamoDB provider/limits; integrator | Table command, strong reads, 8 KiB metadata and transaction cap are AWS-specific; “Complete Example” omits required build/updateStrategy. | Keep DynamoDB reference; use untagged stable install commands and complete required build/updateStrategy fields in the example. |
 | `database-plugins/cloudflare.mdx` | Configure D1 API or Worker binding; integrator | Root versus `/worker` runtime boundary and schema readiness are distinct from managed setup. | Keep focused configuration/contract reference in its independent plugin reference group; use untagged stable install commands. No tutorial merge. |
 | `database-plugins/custom-database.mdx` | Implement provider/conformance; plugin author | 448 lines; repeats Release models/CAS/catalog/schema from normative contract, but detailed Insights semantics and conformance recipe are unique. | Keep implementation/conformance guide; identify private test-utils as a repository workspace helper and verify its command. Large normative-content consolidation deferred to preserve exact contracts and anchors. |
 | `database-plugins/firestore.mdx` | Configure Firestore/ADC; integrator | New-private-key instruction at 36 conflicts with ADC guidance; env-block values are not loaded by shown snippet; duplicate firebase-admin install at 93. | Keep Firestore reference; use untagged stable install commands, reuse ADC and clarify conditional credential-file loading. |
 | `database-plugins/release-catalog-contract.mdx` | Normative persistence/CAS contract; provider author | Explicit normative page has useful exact semantics/acceptance; Status references PR #1141 and removed APIs while latest should describe current model. | Keep normative contract; replace PR-history introduction with current v1 scope. Large contract merge rejected. |
 | `database-plugins/standalone.mdx` | Configure remote management repository and implement its transport; CLI integrator/server author | 436 lines mix repository options with unrelated remote storage (54–102); full contract is valuable; documents no Insights/key proxy correctly. | Keep canonical database options/transport reference; extract standaloneStorage into Storage Plugins and retain the original remote-storage anchor as a compatibility link. |
 | `database-plugins/supabase.mdx` | Configure Postgres RPC adapter and Edge export; integrator | Explicit existing-schema requirement and root/edge boundary useful; generic “Complete Example” partial fields. | Keep focused configuration/contract reference in its independent plugin reference group; use untagged stable install commands. No tutorial merge. |
 | `get-started/basic-usage.mdx` | Manual integration; currently also agent bootstrap, native and deploy | 619 lines mixes three tasks; untagged CLI (19); native setup dominates (283–567); release test late optional link (618). | Split into this ordered manual hub, installation and app-setup; verification lives at `get-started/test-ota.mdx`. Preserve previous setup section anchors. |
 | `get-started/introduction.mdx` | New/evaluating user landing | No actionable start, no version/upgrade boundary, begins with video and CodePush comparison (7–26). | Rewrite as Start here at the same URL; direct agent/manual/migration choices and completion contract. |
 | `guides/agent-infrastructure.mdx` | Agent/operator needing scaffold contract, resume and infra upgrade | Detailed output/resume/evidence content is valuable (56–77, 114–178); not appropriate duplicate quickstart. | Keep detailed scaffold/resume/upgrade contract under Self Hosting (Managed); use canonical installation and agent entry. |
 | `guides/ai-agents.mdx` | Human directing a coding agent | Prompt only provisions infrastructure; repeats reference; does not give verified first OTA contract (23–59). | Rewrite/promote to Start here; complete install-to-native-to-verified-OTA request; detailed scaffold reference stays separate. |
 | `guides/bundle-diffing.mdx` | Operator optimizes and diagnoses artifact downloads | Useful defaults/byte-route/fallback detail (70–106), but repeats usage/concept several times. | Keep distinct task and existing URL; place in Deliver updates or Operate. Additional prose/recipe restructuring deferred. |
 | `guides/bundle-signing.mdx` | App owner configures trusted artifacts; CI owner/KMS/service implementer | Local baseline buried line424; 659 lines mix 4 personas; repeated native anchoring. | Split four signer recipes into guides/signing; keep shared trust-anchor rollout, rotation and troubleshooting. Preserve original heading anchors. |
 | `guides/channel-management.mdx` | Embed default channel before native build | Native and Expo settings are valuable; repeats all release-build commands (126–165); full wrapper omits managed key (200). | Keep default-channel task; preserve authenticated client setup in example. Broader release-command deduplication not included. |
 | `guides/compression.mdx` | Operator adjusts archive bandwidth/CPU | 44-line page repeats deploy boilerplate; absolute compression ratio claims (10–11) unsupported/workload dependent; no default or benchmark method. | Keep distinct task and existing URL; place in Deliver updates or Operate. Additional prose/recipe restructuring deferred. |
 | `guides/console-deployment/cloudflare.mdx` | Operator deploys Console using native D1/R2 bindings | Binding/runtime/secrets/generated Wrangler config workflow materially differs from Node hosts (31–96). | Keep host-specific Console recipe under Operate; preserve URL and host order. Canonical folder index and Markdown handled by shared generator. |
 | `guides/console-deployment/docker.mdx` | Operator hosts Console in container | Docker lockfile (38), loopback/HTTPS, runtime secret mount, architecture and rollback are host-specific. | Keep host-specific Console recipe under Operate; preserve URL and host order. Canonical folder index and Markdown handled by shared generator. |
 | `guides/console-deployment/index.mdx` | Team hosts authenticated persistent Console | Already a coherent clone → plugins → auth → host → verify flow. “Basics” title meaningless outside parent; complete plugin config absent but provider examples linked. | Keep host-specific Console recipe under Operate; preserve URL and host order. Canonical folder index and Markdown handled by shared generator. |
 | `guides/console-deployment/netlify.mdx` | Operator deploys Console to Netlify functions | Preset/publish directory/functions scope (29,36) differs from Vercel; currently concise 62 lines. | Keep host-specific Console recipe under Operate; preserve URL and host order. Canonical folder index and Markdown handled by shared generator. |
 | `guides/console-deployment/vercel.mdx` | Operator deploys Console to Vercel functions | Output directory/preset/prod deploy and download limits are host-specific (35,54,59). | Keep host-specific Console recipe under Operate; preserve URL and host order. Canonical folder index and Markdown handled by shared generator. |
 | `guides/console.mdx` | Operator opens/configures Console and changes release state | Mixes local launch, hosting auth, CLI ops, 50-line metrics reference and promotion. | Split Insights into guides/insights; retain Console management and original Insights anchor as a link. Clarify force-update timing. |
 | `guides/custom-update.mdx` | Developer implements own update lifecycle/UI | Correct null-vs-failure distinction (83), result helper usage (97), full lifecycle. ~135-line UI sample dominates; channel sections duplicate canonical guide. | Promote custom update flow under Start here as the default init-based integration; retain detailed timing/progress/error handling and link references to this owner. |
 | `guides/deploy.mdx` | Operator/CI publishing bundles | Wrong onboarding prerequisite link to Supabase only (19); examples repeat flags and nonexecutable placeholder platform strings (110 etc). Actual ID/rollout defaults valuable. | Keep under Deliver updates; correct provider-neutral setup and init-based custom-flow prerequisites, link first-OTA test. Keep existing option reference rather than another release-management page. |
 | `guides/doctor.mdx` | Diagnose local/infra issues or automate recovery | Clear `--json` nonprompt behavior and fixability boundaries; same-version advice wrong (124), sample output stale compared with semantic UI source. | Keep under Operate; clarify stable compatibility, installation/verification and Catalog preflight/rebuild with backup recovery. No runtime doctor change. |
 | `guides/infrastructure-templates.mdx` | Operator extracting/deploying server templates without agent | Distinct nonexecuting extractor contract (18–20) and provider escaping formats (54–68) cannot disappear into agent prose. | Keep extraction reference under Self Hosting (Managed); explicit v1 CLI prerequisite and shared install link. |
 | `guides/native-build.mdx` | Optional experimental Hot Updater native build CLI user | Different from required AppDelegate integration; stays separate. Example calls nonexistent configured `debugApk` (136 vs config 28–61); default-channel prerequisite (18) conflicts with default production behavior. Not listed in current guides metadata. | Keep optional tooling guide and add missing navigation entry; correct scheme example and default-channel prerequisite. |
 | `guides/rollout-cohorts.mdx` | QA/release manager controlling audience | Exact stable seeded selection and custom-cohort union semantics needed; 411 screenshot flow repeats itself; line14 calls selected Bundle “immutable Bundle,” confusing identity. | Keep distinct task and existing URL; place in Deliver updates or Operate. Additional prose/recipe restructuring deferred. |
 | `guides/runtime-channel-switch.mdx` | App developer temporarily opting a device into beta | Distinct persisted-after-install/reset lifecycle (14–20, 110–138). Correctly more than a build-time channel setting. | Keep distinct task and existing URL; place in Deliver updates or Operate. Additional prose/recipe restructuring deferred. |
 | `guides/simulator-test.mdx` | First OTA validation/local QA | Release build commands useful but no edit/ID/evidence goal; deployment unscoped; title “Simulate Test” conceals first-run requirement. | Rename to `get-started/test-ota.mdx` with no old URL alias. Use four short, observable steps for a Release baseline, visible JS/asset update, application and rollback; remove the protocol probe and link to focused diagnostics. |
 | `guides/storage-cleanup.mdx` | Operator retires bundles and reclaims objects | Strong dry-run/reference-aware/shared-prefix constraints; migration-only S3 sentence (25) strays into history. | Keep distinct task and existing URL; place in Deliver updates or Operate. Additional prose/recipe restructuring deferred. |
 | `guides/update-strategies/app-version.mdx` | Developer chooses explicit native version compatibility | Default strategy is established; dangerous broad wildcard example without native compatibility caveat; duplicate unauthenticated wrapper/curl. | Keep separate strategy alternative; preserve client auth and distinguish unauthenticated 401 from catalog/OTA verification. |
 | `guides/update-strategies/fingerprint.mdx` | Developer opts into native fingerprint targeting | Workflow/native regeneration and extras need dedicated reference; “only safe updates” (11) overclaims what hashing can prove; missing auth examples. | Keep separate strategy alternative; preserve client auth and distinguish unauthenticated 401 from catalog/OTA verification. |
 | `guides/upgrade-to-v1.mdx` | Existing v0 operator | Strong separate-native/new-endpoint/data preservation and device tests (12–29, 72–98, 190–225). This is the one place v0 transitions belong. | Keep under Start here; preserve migration safety and add the complete server/SDK/provider/CLI contract migration mapping. |
 | `integration-plugins/bugsnag.mdx` | Upload/map OTA sourcemaps; app observability owner | Uses manifest artifact ID, matching plugin's bundle-ID default; three build tabs genuinely aid selection. | Keep independently searchable sourcemap integration; use untagged stable install commands and add an actual symbolication verification outcome. |
 | `integration-plugins/datadog.mdx` | Upload OTA sourcemaps; app observability owner | Requires buildNumber/releaseVersion/service matching runtime; jumps Step 1 to Step 3 with no runtime-matching recipe or success verification. | Keep independently searchable sourcemap integration; use untagged stable install commands and add an actual symbolication verification outcome. |
 | `integration-plugins/sentry.mdx` | Configure debug IDs/upload sourcemaps; app observability owner | Metro/Re.Pack debug-ID setup is unique and plugin reads debug IDs; stops at deploy rather than verified symbolication. | Keep independently searchable sourcemap integration; use untagged stable install commands and add an actual symbolication verification outcome. |
 | `managed/aws.mdx` | Provision and operate AWS OTA; app owner/agent | 710 lines; native integration starts at 414, verification at 703 says dashboard/test app; cost/capacity at 242 is useful AWS-specific operation knowledge. | Merge repeated runtime/native integration into app-setup; keep provider-specific setup, exact URL/client-key outputs and original Step 3/4/5 link anchors. Require doctor + OTA handoff; use untagged stable install commands. |
 | `managed/cloudflare.mdx` | Provision D1/R2/Worker; app owner/agent | 577 lines; native code starts 281, end at 570 lacks executable finish; API versus R2 credentials and Worker cache verification are provider-specific. | Merge repeated runtime/native integration into app-setup; keep provider-specific setup, exact URL/client-key outputs and original Step 3/4/5 link anchors. Require doctor + OTA handoff; use untagged stable install commands. |
 | `managed/firebase.mdx` | Provision Firestore/Storage/Function; app owner/agent | 581 lines; prerequisites 22–25 ask for precreated project/Storage/Firestore despite agent creating them; ADC reuse at 190 and origin-only cache are useful. | Merge repeated runtime/native integration into app-setup; keep provider-specific setup, exact URL/client-key outputs and original Step 3/4/5 link anchors. Require doctor + OTA handoff; use untagged stable install commands. |
 | `managed/supabase.mdx` | Provision Postgres/Storage/Edge Function; app owner/agent | 582 lines; native code starts 286; resource names and origin-only cache at 220–250 prevent incorrect CDN claims. | Merge repeated runtime/native integration into app-setup; keep provider-specific setup, exact URL/client-key outputs and original Step 3/4/5 link anchors. Require doctor + OTA handoff; use untagged stable install commands. |
 | `policy/app-review.mdx` | Native compatibility and store policy boundary; release owner | Authoritative Apple/Google links present and appropriately warns technical compatibility does not imply review exemption. Repeated long examples obscure decision. | Reorganize native-compatibility decision and existing policy links; preserve separate ownership and avoid approval guarantees. |
 | `policy/security.mdx` | Credential exposure and trust boundaries; app/server implementer + agent | Correct distinction client key vs provider credential buried after 50 lines. Focus on environment inliners leaves admin/signing/reporting structure implicit. Line 79 contains raw non-comment annotation in JS. | Rewrite around credential/trust boundary matrix; link setup, signing and Insights. Preserve plugin-token-protection anchor. |
 | `react-native-api/addListener.mdx` | Raw progress events and side effects; integrator | Complete discriminated progress event, cleanup example, and correct hook recommendation. | Keep focused symbol reference unchanged; categorize under API navigation/overview. Further optional sample trimming is deferred. |
 | `react-native-api/checkForUpdate.mdx` | Select a compatible transition; mobile implementer / runtime debugger | Correct null/error distinction and lazy artifact warning at lines 143–156. Two long near-identical manual examples obscure parameters; returned v1 fields `transitionKind`, `releaseId`, cohort targeting absent from result reference. | Keep selection API; reduce duplicate tutorial, document public transition fields and share authenticated init configuration. |
 | `react-native-api/clearCrashHistory.mdx` | Allow retrying locally rejected artifacts; support/QA | Correct boolean return, but “allow redeploying” suggests server action; actually device-local rejection list. First sample uses View/Button without importing them. | Keep symbol URL; clarify local retry effect and replace incomplete example. |
 | `react-native-api/getAppVersion.mdx` | Inspect native compatibility version; developer/support | Correct sync nullable result and platform sources. Focused; no wrong behavior found. | Keep focused symbol reference unchanged; categorize under API navigation/overview. Further optional sample trimming is deferred. |
 | `react-native-api/getBundleId.mdx` | Inspect selected console update ID; support/operator | Correct crucial v1 distinction: public getter returns selected Release ID, may change before reload. Needs companion state/reference links because artifact ID is different. | Keep selected-ID contract; link active-state and artifact/crash diagnostics. Selected does not imply running. |
 | `react-native-api/getChannel.mdx` | Inspect effective delivery channel; mobile debugger | Source matches runtime/default semantics. Full root wrap example and runtime switch example duplicate workflow. | Keep focused symbol reference; remove unnecessary root wrapper and show runtime checks after init with successful-download guarding. |
 | `react-native-api/getCohort.mdx` | Inspect persisted rollout cohort; operator / QA mobile implementer | Correct persisted initial cohort and 1..1000/custom behavior; no signature showing string return. | Keep focused symbol reference unchanged; categorize under API navigation/overview. Further optional sample trimming is deferred. |
 | `react-native-api/getCrashHistory.mdx` | Inspect rejected artifacts after recovery; support/QA | Correct artifact-vs-console-ID explanation. 141 lines for a getter, 4 redundant monitoring/log examples; first example missing View/Button imports; Related erroneously says wrap is required though init works. | Keep symbol URL; replace redundant/broken UI samples with one complete local-artifact diagnostic example. |
 | `react-native-api/getDefaultChannel.mdx` | Read embedded channel despite switch; mobile debugger | Small, focused, correct comparison to getChannel/isChannelSwitched. | Keep focused symbol reference unchanged; categorize under API navigation/overview. Further optional sample trimming is deferred. |
 | `react-native-api/init.mdx` | Manual lifecycle initialization; mobile implementer | Correct no-check behavior and Insights default; lines 17–28 reproduce schema migration + client-key creation belonging to backend access setup. BaseURL type wrong at line 89. | Keep symbol URL; link canonical key bootstrap/manual flow, correct baseURL type and shared authentication. |
 | `react-native-api/isChannelSwitched.mdx` | Inspect override status; debug UI implementer | Distinct boolean query, correct simple definition. Runtime example should not claim true after a null/failed update. | Keep focused symbol reference unchanged; categorize under API navigation/overview. Further optional sample trimming is deferred. |
 | `react-native-api/isUpdateDownloaded.mdx` | Nonreactive current-session readiness; imperative integrator | Correct derived, session-scoped signal. UI example duplicates store page; readiness is not full persisted update state. | Keep focused symbol reference unchanged; categorize under API navigation/overview. Further optional sample trimming is deferred. |
 | `react-native-api/reload.mdx` | Start selected/staged runtime; mobile implementer | Default processRestart / iOS behavior source-verified. Dangling line “This is the most common manual usage pattern:” at line 32; “old in-process” is unnecessary transition framing. | Keep symbol URL; tighten current behavior wording and remove dangling example introduction. |
 | `react-native-api/resetChannel.mdx` | Reset OTA state to original native bundle; QA / support implementer | Correctly warns it clears OTA state even with no runtime switch. This destructive local behavior deserves its own discoverable reference. | Keep focused symbol reference unchanged; categorize under API navigation/overview. Further optional sample trimming is deferred. |
 | `react-native-api/setCohort.mdx` | Override rollout eligibility for QA/targeting; mobile implementer | Says “Accepts numeric cohorts” but actual argument is string; a user could pass `500` and fail typechecking. Existing custom-string example is valid. | Keep state-changing symbol URL; clarify string argument and numeric-cohort representation. |
 | `react-native-api/setReloadBehavior.mdx` | Android restart choice / brownfield hook; native integrator | Useful distinct configuration API with platform rationale; Supported behaviors says “Android only” although both modes are callable and fall back to normal reload on iOS. | Keep symbol URL; clarify behavior on both platforms. |
 | `react-native-api/updateBundle.mdx` | Stage downloaded content or selected transition; advanced SDK user | Correct helper-first and no-reload semantics. Parameter table admits null fileHash without stating downloads reject it; hash verification prose omits that configured public key rejects unsigned hashes. | Keep staging API and helper-first flow; clarify direct-call hash/signing rules and shared initialization headers. |
 | `react-native-api/useHotUpdaterStore.mdx` | Render download UI; React developer | Correct fields and selector usage, meaningfully different from nonreactive getter and raw events. | Keep focused symbol reference unchanged; categorize under API navigation/overview. Further optional sample trimming is deferred. |
 | `react-native-api/wrap.mdx` | Automatic startup integration; mobile implementer / coding agent | Lines 59–116 duplicate a full manual init/check flow; lines 346–366 repeat the protocol contract. Important force/staging and completion-callback timing are already correct. Options miss new fallback progress fields, and baseURL type needs parentheses. | Keep symbol URL; remove duplicated manual tutorial/protocol detail, link canonical owners, correct options/fallback fields. |
 | `storage-plugins/aws.mdx` | Configure S3/S3-compatible object storage; integrator | Signed-download configuration and storage-prefix prune ownership (85–103) are separate from AWS managed provisioning. | Keep focused configuration/contract reference in its independent plugin reference group; use untagged stable install commands. No tutorial merge. |
 | `storage-plugins/cloudflare.mdx` | Configure R2 S3 transport; integrator | 72 focused lines; `r2://` differs from `s3Storage` on R2; line 37 says fallback “is removed,” a migration comparison in latest reference. | Keep R2 reference; use untagged stable install commands and describe current credential requirement without migration comparison. |
 | `storage-plugins/custom-storage.mdx` | Implement storage provider; plugin author | Flat Web-stream contract, URI ownership, capability matrix and exact-delete semantics are relevant standalone contract knowledge. | Keep focused configuration/contract reference in its independent plugin reference group; use untagged stable install commands. No tutorial merge. |
 | `storage-plugins/firebase.mdx` | Configure Firebase Storage; integrator | Env block at 39 tells everyone to download new private key; managed guide/agent environment correctly use existing ADC conditionally. | Keep Firebase storage reference; use untagged stable install commands, reuse ADC and clarify conditional credential-file loading. |
 | `storage-plugins/supabase.mdx` | Configure Supabase storage; integrator | Small config/protocol/runtime reference with useful edge subpath and buffering behavior. | Keep focused configuration/contract reference in its independent plugin reference group; use untagged stable install commands. No tutorial merge. |

## New canonical pages

The restructuring adds **14** focused pages, for **100** latest pages total. New pages either own extracted content or close an onboarding/v1 reference gap.

| Page | Role |
| --- | --- |
| `get-started/app-setup.mdx` | Shared app/runtime/native integration, extracted from Basic Usage and four provider recipes. |
| `get-started/choose-provider.mdx` | Provider/custom-server choice before provisioning. |
| `get-started/installation.mdx` | One package/channel/prerequisite contract for both setup paths. |
| `guides/insights.mdx` | Independent default-on reporting, identity, verification and metrics task, extracted from Console. |
| `guides/signing/aws-kms.mdx` | Independent signer setup recipe extracted from bundle-signing; shared native trust-anchor lifecycle remains at the original URL. |
| `guides/signing/google-cloud-kms.mdx` | Independent signer setup recipe extracted from bundle-signing; shared native trust-anchor lifecycle remains at the original URL. |
| `guides/signing/local.mdx` | Independent signer setup recipe extracted from bundle-signing; shared native trust-anchor lifecycle remains at the original URL. |
| `guides/signing/remote.mdx` | Independent signer setup recipe extracted from bundle-signing; shared native trust-anchor lifecycle remains at the original URL. |
| `react-native-api/getActiveUpdateState.mdx` | Public v1 active/stable selection and pending-verification contract. |
| `react-native-api/getInstallId.mdx` | Public installation identity lookup for support and Insights. |
| `react-native-api/index.mdx` | Categorized entry to focused API references. |
| `react-native-api/notifyAppReady.mdx` | Complete launch-result union and callback/direct-read semantics, identified by the BREAKING_CHANGES audit. |
| `react-native-api/setUser.mdx` | Public user identity update/clearing contract for future reports. |
| `storage-plugins/standalone.mdx` | Standalone storage transport, extracted from the database repository reference into its own concern. |

## Scope boundaries

- v0 MDX and metadata remain unchanged. README/release blog and historical architecture/measurement content remain unchanged.
- Latest-only metadata and machine-output validation are maintained with the site. The standalone historical measurement directory is excluded from the site TypeScript check; the measurement files themselves are unchanged.
- Deferred content is available at its existing URL; no page was silently removed. Deferred optional splits do not block the accepted installation-to-verification workflow.

The follow-up [breaking-change coverage matrix](breaking-changes-coverage.md) records the v1 contract audit and additional corrections. General SDK examples now prefer init with explicit checking, download and reload; the wrap API remains available for automatic startup updates.
