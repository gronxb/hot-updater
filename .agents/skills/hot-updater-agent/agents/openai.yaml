interface:
  display_name: "HotUpdater Agent"
  short_description: "Run dashboard E2E through the local CLI"
  default_prompt: "Use $hot-updater-agent to run `hot-updater-agent verify ...`, inspect failures with `reason <task-id>`, and diagnose provider bottlenecks with `hot-updater-agent -json timeline <profile> -limit 10`."
