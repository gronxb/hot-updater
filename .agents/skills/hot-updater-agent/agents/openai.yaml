interface:
  display_name: "HotUpdater Agent"
  short_description: "Run full E2E or prepare a manual QA environment"
  default_prompt: "Use $hot-updater-agent to choose full `verify` or a leased `manual start` environment. For manual QA consume the JSON handoff commands and always call `manual stop`."
